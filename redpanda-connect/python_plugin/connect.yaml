input:
  generate:
    mapping: |
      root = {
        "message": fake("sentence")
      }
    interval: ""
    count: 10_000

pipeline:
  processors:
    - yell: {}

output:
  file:
    codec: lines
    path: ./processed/output.jsonl
